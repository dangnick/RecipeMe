//
//  ContentView.swift
//  RecipeMe
//
//  Created by Nicholas Dang on 3/10/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Changing text once")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
